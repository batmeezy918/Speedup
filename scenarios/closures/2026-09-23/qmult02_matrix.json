{
  "schema": "PCSS-SCENARIO",
  "schema_version": "1.0",
  "scenario_id": "qmult02-quotient-gemm-2026-09-23",
  "workload": {
    "kind": "exact_invariant_sector",
    "function_name": "engines.linear_exact.LinearExactMatrixModel",
    "model": "matrix"
  },
  "mathematical_object": {
    "state_space": "R^(n x n) with tile-tile block-constancy Omega; block class c=(a*bi+b*bj) mod nclasses",
    "quotient_space": "R^r block representatives (bi,bj) -> A[bi*tile][bj*tile]",
    "transition": "T(A)[ii][jj] = A[ii][jj] * w[class(bi,bj)]  (block-scale, block-constant preserving)",
    "projection": "pi(A) = reps over blocks, one rep per tile-tile block",
    "reconstruction": "sigma(q) = replicate each rep across its tile-tile block (exact)",
    "invariant": "tile-tile block constancy (omega_matrix exact)",
    "observable": "class representative matrix pi(A)"
  },
  "baseline": {
    "implementation_id": "QMULT02_S_SANDBOX_full_1024",
    "kind": "native"
  },
  "candidate": {
    "implementation_id": "QMULT02_S_SANDBOX_quotient_r64",
    "kind": "native"
  },
  "quotient": {
    "definition": "exact tile-tile block-constant projection; pi(T(A)) = Tbar(pi(A)) on sector (intertwining)",
    "function_name": "engines.quotient.run_quotient",
    "tolerance": 0.0,
    "relation": "exact"
  },
  "reconstruction": {
    "definition": "sigma(q) block-replicate each quotient rep back to n x n full matrix (section: pi(sigma(q))=q)",
    "function_name": "engines.reconstruction.run_reconstruction",
    "tolerance": 0.0,
    "metric": "max"
  },
  "invariant": {
    "definition": "tile-tile block constancy on every trajectory state (exact for invariant sector)",
    "function_name": "engines.linear_exact.omega_matrix"
  },
  "parameters": {
    "model": "matrix",
    "steps": 8,
    "tile": 16,
    "a": 3,
    "b": 1,
    "weights": [
      1.0,
      1.25,
      0.9,
      1.1,
      1.5,
      0.8,
      1.3,
      0.95
    ],
    "note": "silicon closure 2026-09-23 QMULT-02 S_SANDBOX: N=1024 r=64 -> 19.99x fp32 / 17.16x fp64; battery 21/21 passing; this PCSS run measures the exact-invariant-sector rewrite on the same block-constant construction"
  },
  "dimensions": {
    "matrix_rows": 512,
    "matrix_cols": 512,
    "d": 512,
    "r": 1024,
    "tile": 16,
    "nclasses": 8,
    "operation_count_baseline_per_step": 262144,
    "operation_count_candidate_per_step": 1024,
    "operation_count_ratio_theory": 256.0
  },
  "repetitions": 30,
  "warmups": 5,
  "warmup_policy": "fixed",
  "timing_source": "clock_gettime_monotonic",
  "seed": 20260923,
  "expected_observables": [
    "quotient forward exact",
    "reconstruction exact",
    "invariant block-constancy exact",
    "direct wall-clock speedup > 1 reported",
    "operation count ratio reported separately"
  ],
  "environment": {
    "python": "3.11",
    "arch": "aarch64"
  }
}