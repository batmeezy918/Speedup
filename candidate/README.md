# Candidate Speedup Claims

This directory contains speed-up artifacts that are reproducible or otherwise evidenced but have not satisfied the complete PCSS publication predicate.

Required transition:

`PUBLISH <=> I ∧ R ∧ Q ∧ Q⁻¹ ∧ Ω ∧ X ∧ L`

Candidate artifacts must never be represented as `VERIFIED`.

Each artifact should carry its canonical scenario ID, manifest hash, artifact hashes, source paths, gate matrix, limitations, and exact claim scope.
