{
  "baseline": {
    "implementation_id": "AQGE02_full_tower_6",
    "kind": "native"
  },
  "candidate": {
    "implementation_id": "AQGE02_stay_in_Q_tower_6",
    "kind": "native"
  },
  "dimensions": {
    "matrix_rows": 512,
    "matrix_cols": 512,
    "d": 512,
    "r": 1024,
    "tile": 16,
    "nclasses": 8,
    "operation_count_baseline_per_step": 262144,
    "operation_count_candidate_per_step": 1024,
    "operation_count_ratio_theory": 256.0
  },
  "environment": {
    "python": "3.11",
    "arch": "aarch64"
  },
  "expected_observables": [
    "quotient forward exact across all 6 powers",
    "reconstruction exact at compose point",
    "invariant block-constancy exact across tower",
    "direct wall-clock speedup > 1 reported",
    "operation count ratio reported separately"
  ],
  "invariant": {
    "definition": "tile-tile block constancy on every tower state (exact)",
    "function_name": "engines.linear_exact.omega_matrix"
  },
  "mathematical_object": {
    "state_space": "R^(n x n) tower block-constant sector Omega (stacked block-scaled matrix)",
    "quotient_space": "R^r block representatives; tower power absorbs multiple factor blocks",
    "transition": "T(A)[ii][jj] = A[ii][jj] * w[class(bi,bj)]  (block-scale; tower keeps state in Q across 6 powers)",
    "projection": "pi(A) = one rep per tile-tile block; state NEVER leaves Q across the tower",
    "reconstruction": "sigma(q) = block-replicate rep back to n x n (only at tower compose point)",
    "invariant": "tile-tile block constancy across all 6 tower powers (exact)",
    "observable": "class representative matrix pi(A)"
  },
  "parameters": {
    "model": "matrix",
    "steps": 6,
    "tile": 16,
    "a": 3,
    "b": 1,
    "weights": [
      1.0,
      1.25,
      0.9,
      1.1,
      1.5,
      0.8,
      1.3,
      0.95
    ],
    "note": "silicon closure 2026-09-23 AQGE-02: power6 (AB)^6 stay-in-Q 457.1x, composed 69.41x, S_native 72.07x, K_break 21.0; this PCSS run measures the exact-invariant-sector stay-in-Q tower on the same construction"
  },
  "quotient": {
    "definition": "exact block-constant projection preserved across every tower power (intertwining per step)",
    "function_name": "engines.quotient.run_quotient",
    "tolerance": 0.0,
    "relation": "exact"
  },
  "reconstruction": {
    "definition": "sigma(q) single compose-time replication; pi(sigma(q))=q section exact",
    "function_name": "engines.reconstruction.run_reconstruction",
    "tolerance": 0.0,
    "metric": "max"
  },
  "repetitions": 30,
  "scenario_id": "aqge02-tower-stay-in-Q-2026-09-23",
  "schema": "PCSS-SCENARIO",
  "schema_version": "1.0",
  "seed": 20260923,
  "timing_source": "clock_gettime_monotonic",
  "warmup_policy": "fixed",
  "warmups": 5,
  "workload": {
    "kind": "exact_invariant_sector",
    "function_name": "engines.linear_exact.LinearExactMatrixModel",
    "model": "matrix"
  }
}
